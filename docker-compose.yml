services:
  openrouter-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openrouter-monitor
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
